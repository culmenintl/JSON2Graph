{
    "name": "api",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "build": "yarn tsc && cp staticwebapp.config.json dist/staticwebapp.config.json",
        "watch": "yarn tsc -w",
        "clean": "rimraf dist",
        "prestart": "yarn clean && yarn build",
        "start": "func start",
        "test": "echo \"No tests yet...\""
    },
    "dependencies": {
        "@azure/functions": "^4.0.0-alpha.7"
    },
    "devDependencies": {
        "@types/node": "^18.x",
        "typescript": "^4.0.0",
        "rimraf": "^5.0.0"
    },
    "main": "dist/src/functions/*.js",
    "engines": {
        "yarn": ">=1.22.0"
    }
}
